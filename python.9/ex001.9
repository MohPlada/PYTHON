"""[Exercício 1] Escreva um programa que tenha uma
tupla totalmente preenchida com uma contagem por
extenso, de zero até vinte. Seu programa deverá
ler um número pelo teclado (entre 0 e 20) e mostrá-lo por extenso."""

extenso = ('zero', 'um', 'dois', 'três', 'quatro', 'cinco', 'seis', 'sete', 'oito', 'nove', 'dez', 'onze', 'doze', 'treze',
'quatorze', 'quinze', 'dezesseis', 'dezessete', 'dezoito', 'dezenove', 'vinte')

pergunta = int(input("Digite o número de 0 a 20 para vê-lo por extenso:"))
while pergunta not in range(0,21):
    pergunta = int(input("Tente novamente.Digite o número de 0 a 20 para vê-lo por extenso:"))

print(f"Você digitou o número {extenso[pergunta]}.")
